<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppMinhasCompras.Views.RelatorioCompras"
             Title="RelatorioCompras">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="VerticalTextAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>
        <Frame Padding="0" Margin="10" VerticalOptions="FillAndExpand">
            <ListView ItemsSource="{Binding Relatorio}">
                <ListView.Header >
                    <Grid ColumnDefinitions="200, 200"
                        BackgroundColor="{StaticResource PrimaryDark}">
                        <Label Grid.Column="0" Text="Categoria" Padding="5" FontAttributes="Bold" HorizontalTextAlignment="Center" TextColor="{StaticResource Primary}"/>
                        <Label Grid.Column="1" Text="Total" FontAttributes="Bold" HorizontalTextAlignment="Center" TextColor="{StaticResource Primary}"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid ColumnDefinitions="200, 200">
                                <Label Grid.Column="0" Text="{Binding Categoria}" Padding="5" HorizontalTextAlignment="Center"/>
                                <Label Grid.Column="1" Text="{Binding Total, StringFormat='{}{0:c}'}" HorizontalTextAlignment="Center"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
    </StackLayout>

   <!---<ContentPage.Content>
        <CollectionView ItemsSource="{Binding Relatorio}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Label Text="{Binding Categoria}" FontSize="18" FontAttributes="Bold"/>
                        <Label Text="{Binding Total, StringFormat='Total: {0:C}'}" FontSize="16" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>11-->

</ContentPage>